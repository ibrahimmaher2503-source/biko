# Biko App Core

Shared mobile domain enums and public environment configuration. Business rules that must stay identical in the User and Driver apps belong here only when both apps actually use them.

```powershell
flutter test
```

