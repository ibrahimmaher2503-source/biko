# Biko Unified Dashboard

One Next.js application for platform and office users. Roles, permissions, and backend data scope will decide what each authenticated user can access.

```powershell
npm install
npm run dev
npm run lint
npm run build
```

Copy `.env.example` to `.env.local` only after a Development Supabase project exists. Never place a service-role key in this browser application.

