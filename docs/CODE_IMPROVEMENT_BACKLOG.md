# Code Improvement Backlog

Review date: 2026-08-30. Original review: **32 findings — P0: 0; P1: 8; P2: 19; P3: 5.** Authorized Waves A/B/C, Evidence Closure, and Milestones 8/9 fully resolved 19 findings; 13 findings remain open, partial, externally pending, or deferred. CR-006 awaits only hosted redirect confirmation. CR-007 is implemented but awaits merged release/device evidence. CR-014/015/016/017 are resolved. Rankings and problem descriptions below are historical review evidence; use the Status column for current state.

Wave A migration: hosted `20260830133522_wave_a_security_transaction_integrity`. Wave B migration: hosted `20260830135840_wave_b_recovery_auth_contracts`. Wave C migration: hosted `20260830143750_wave_c_read_correctness`. Evidence Closure resolved CR-008 through a genuine overlapping hosted application-path race and resolved CR-024 through focused documentation reconciliation. [Implementation Progress](C:/projects/biko/docs/IMPLEMENTATION_PROGRESS.md) records the current Gate 2 PASS WITH ISSUES decision and User App Core unblocking.

The detailed report contains exact source traces, protections, reproducible rollback probes, confidence boundaries, and acceptance criteria: [Deep Code Review Report](C:/projects/biko/docs/DEEP_CODE_REVIEW_REPORT.md).

## Top 15 Highest-Value Improvements

Rank reflects correctness, security, reliability, effort and prevention of duplicated patterns; it is not an instruction to implement a whole wave automatically.

1. **CR-001 — Permissions from one office authorize another office** (P1, ROI-A). Resolve office membership, its role permission, and target_office_id in the same EXISTS. Keep explicit platform Super Admin handling separate.
2. **CR-002 — Suspension does not revoke privileged reads** (P1, ROI-A). Require an ACTIVE caller profile in privilege helpers and an ACTIVE target office for office-scoped access. Make any exceptional suspended-office support access explicit and platform-only.
3. **CR-003 — A request waiting on locks can act after the bidding deadline** (P1, ROI-A). Check clock_timestamp() after acquiring the locks needed for the write, immediately before the state mutation. Apply the same deadline discipline to submit/withdraw; leave stable read-time queries alone.
4. **CR-004 — create_order has no stable key for recovering an uncertain creation** (P1, ROI-A). Add one caller-generated creation intent UUID, unique per customer, and return the existing result for the same intent; reject payload changes for that key. New intentional bookings use new keys.
5. **CR-005 — A stalled request can hold the entire Driver app behind a permanent overlay** (P1, ROI-A). Use the installed client's bounded request/abort support, choose a bounded read retry budget, and enter uncertain/read-only recovery on mutation timeout. Never treat abort as proof the database rolled back or replay a sensitive RPC automatically.
6. **CR-008 — The changed cross-order acceptance lock protocol lacks a genuine race proof** (P1, ROI-A). Run one barrier-controlled two-session test through an explicitly authorized concurrency-capable path; retain timestamps, both outcomes, final invariant/event counts, and cleanup. Do not add infrastructure implicitly.
7. **CR-006 — Forgot Password sends email but cannot finish resetting the password** (P1, ROI-A). Complete the existing shared Auth flow with a recovery callback, recovery-mode routing, new-password submission, and allowlisted mobile redirect configuration. Do not introduce login OTP.
8. **CR-007 — Release manifests do not declare INTERNET permission** (P1, ROI-A). Declare INTERNET in each main manifest and verify the merged release manifest once. Do not rely on a dependency to grant an app-required permission.
9. **CR-012 — Active-trip restoration and Profile depend on the whole Dashboard** (P2, ROI-A). Restore from the minimal account/active-order read; make optional feed failures local to their section. Profile/sign-out must not depend on available requests. Reuse existing services, without adding repository layers.
10. **CR-011 — Waiting details treat offer status as complete order truth** (P2, ROI-A). Read one owned-offer summary with authoritative order status and expiry; derive waiting/selected/terminal display together. Keep terminal orders terminal and remove stale actions.
11. **CR-010 — Completed-trip count never changes after completion** (P2, ROI-A). Choose one authority: update the count transactionally once per completion with a reconciliation/backfill rule, or derive the actual count in a measured set-based read. Do not invent ratings.
12. **CR-009 — A suspended customer can still select a Driver** (P2, ROI-A). Require ACTIVE CUSTOMER eligibility when accepting an offer. Preserve any deliberately allowed safe cancellation/history access separately.
13. **CR-018 — Application Auth subscriptions do not handle stream errors** (P2, ROI-A). Handle the Auth error channel in each application subscription, retaining the session on retryable network failure and exposing safe recovery. Keep invalid-session sign-out distinct.
14. **CR-021 — Most completed backend contracts have evidence but no retained runnable regression** (P2, ROI-A). Retain a compact rollback-based contract test per changed trust boundary, including mixed-role and deadline negatives. Run affected cases, not a full gate every time.
15. **CR-022 — Fakes bypass the real service, router, and failure composition** (P2, ROI-A). Keep the good tests; add a small mocked-HTTP service contract test and one configured root-router Auth/session test. Include commit-later and never-settling reads, not only commit-before-timeout.

## Improvement Backlog

ROI-A: high impact / low–medium effort. ROI-B: high impact / high effort. ROI-C: medium impact / low effort. ROI-D: low priority / defer. Scope estimates are change-size judgments, not delivery promises. No broad refactor/new infrastructure project is recommended.

| ID | Severity | ROI | Area | File / Function | Finding | Evidence | Impact | Recommended Change | Fix Timing | Estimated Scope | Dependency | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CR-001 | P1 | ROI-A | Office authorization; SECURITY RISK | [supabase/migrations/202608300001_authorization_rls.sql](C:/projects/biko/supabase/migrations/202608300001_authorization_rls.sql:66) — private.can_access_office / private.has_permission | Permissions from one office authorize another office | can_access_office checks target membership, then has_permission searches all memberships (lines 22–80). Hosted probe: Accountant A + Dispatcher B read 1 Driver-document row in A. | Cross-office permission escalation exposes driver profiles and document metadata within an office where the user lacks that permission. | Resolve office membership, its role permission, and target_office_id in the same EXISTS. Keep explicit platform Super Admin handling separate. | FIX NOW BEFORE USER APP | Small helper migration + mixed-role regression | None | RESOLVED — Wave A hosted targeted PASS (20260830133522) |
| CR-002 | P1 | ROI-A | Privilege revocation; SECURITY RISK | [supabase/migrations/202608300001_authorization_rls.sql](C:/projects/biko/supabase/migrations/202608300001_authorization_rls.sql:5) — private.is_super_admin / has_permission / is_office_member | Suspension does not revoke privileged reads | Helpers inspect role/membership rows but never profiles.status or offices.status. Hosted probes returned one protected document for suspended staff and suspended-office staff; suspended Super Admin still returned true and read it. | Suspended privileged accounts retain access to other people's operational/private data. | Require an ACTIVE caller profile in privilege helpers and an ACTIVE target office for office-scoped access. Make any exceptional suspended-office support access explicit and platform-only. | FIX NOW BEFORE USER APP | Small helper migration + suspension cases | Coordinate with CR-001 | RESOLVED — Wave A hosted targeted PASS (20260830133522) |
| CR-003 | P1 | ROI-A | Bidding transactions; LOGIC DEFECT | [supabase/migrations/202608300008_gate_2_remediation.sql](C:/projects/biko/supabase/migrations/202608300008_gate_2_remediation.sql:216) — accept_offer; submit_offer; withdraw_offer | A request waiting on locks can act after the bidding deadline | Expiry compares bidding_expires_at to transaction-start now(); accept_offer checks before later Driver/offer locks. Rollback probe: wall clock passed expiry, yet accept_offer returned DRIVER_ASSIGNED. | The frozen 90-second eligibility boundary is exceeded under contention, even with one valid winner. | Check clock_timestamp() after acquiring the locks needed for the write, immediately before the state mutation. Apply the same deadline discipline to submit/withdraw; leave stable read-time queries alone. | FIX NOW BEFORE USER APP | Focused RPC migration + boundary test | Preserve current lock order | RESOLVED — Wave A hosted targeted PASS (20260830133522) |
| CR-004 | P1 | ROI-A | Create/recovery contract; ARCHITECTURE DEBT | [supabase/migrations/202608300007_user_flow_privacy_hardening.sql](C:/projects/biko/supabase/migrations/202608300007_user_flow_privacy_hardening.sql:63) — create_order | create_order has no stable key for recovering an uncertain creation | Every call inserts a generated order ID; there is no client intent ID/uniqueness contract. Two identical authenticated calls in the rollback probe produced distinct orders. | Duplicate bookings or ambiguous recovery; copying the Driver read-after-write pattern cannot identify a new order whose ID was never received. | Add one caller-generated creation intent UUID, unique per customer, and return the existing result for the same intent; reject payload changes for that key. New intentional bookings use new keys. | FIX NOW BEFORE USER APP | Focused contract migration + lost-response test | Customer creation UI consumes the contract later | RESOLVED — Wave B hosted targeted PASS (`20260830135840`) |
| CR-005 | P1 | ROI-A | Network recovery; UX/STATE LOGIC ISSUE | [packages/app_core/lib/operation_recovery.dart](C:/projects/biko/packages/app_core/lib/operation_recovery.dart:94) — MutationReconciler.run / retryRead | A stalled request can hold the entire Driver app behind a permanent overlay | await mutate() and await read() are unbounded; ConnectionStateView hides retry while checking/submitting. DriverRecoveryBoundary absorbs all actions. Installed PostgREST 2.9.1 defaults requestTimeout to null; app initialization supplies no override. | Active-trip controls, navigation, and Profile sign-out remain inaccessible for an unbounded period. | Use the installed client's bounded request/abort support, choose a bounded read retry budget, and enter uncertain/read-only recovery on mutation timeout. Never treat abort as proof the database rolled back or replay a sensitive RPC automatically. | FIX NOW BEFORE USER APP | Small transport/recovery configuration + stalled-Future test | CR-004 for safe creation recovery | RESOLVED — Wave B targeted Flutter PASS |
| CR-006 | P1 | ROI-A | Shared authentication; BUG | [packages/app_core/lib/app_core.dart](C:/projects/biko/packages/app_core/lib/app_core.dart:113) — AuthService.requestPasswordReset / createAuthRouter / AuthMode | Forgot Password sends email but cannot finish resetting the password | Only resetPasswordForEmail exists; no passwordRecovery route, new-password form, or auth.updateUser call exists anywhere in app code. Mobile manifests/plists have no recovery callback configuration; local site_url points to 127.0.0.1:3000. | The implemented account-recovery feature cannot complete in either mobile app. | Complete the existing shared Auth flow with a recovery callback, recovery-mode routing, new-password submission, and allowlisted mobile redirect configuration. Do not introduce login OTP. | FIX NOW BEFORE USER APP | Shared Auth UI/router + Android/iOS callback setup | Approved Development Auth redirect configuration | IMPLEMENTED — HOSTED REDIRECT CONFIRMATION PENDING |
| CR-007 | P1 | ROI-A | Android release configuration; BUG | [apps/driver_app/android/app/src/main/AndroidManifest.xml](C:/projects/biko/apps/driver_app/android/app/src/main/AndroidManifest.xml:1) — Main manifests in both Flutter apps | Release manifests do not declare INTERNET permission | INTERNET exists only in src/debug and src/profile, not src/main. Both apps' resolved Android plugin manifests (app_links, shared_preferences_android, url_launcher_android) also omit it. | Supabase authentication and database calls cannot work in the release app. | Declare INTERNET in each main manifest and verify the merged release manifest once. Do not rely on a dependency to grant an app-required permission. | FIX BEFORE PUBLIC LAUNCH | Two manifest lines + merged-manifest check | Android release toolchain for acceptance | IMPLEMENTED — merged release-manifest/device acceptance pending |
| CR-008 | P1 | ROI-A | Concurrency evidence; TESTABILITY GAP | [supabase/migrations/202608300008_gate_2_remediation.sql](C:/projects/biko/supabase/migrations/202608300008_gate_2_remediation.sql:184) — accept_offer; supabase/tests/gate_2_remediation.sql | The changed cross-order acceptance lock protocol lacks a genuine race proof | Current code has Driver locking/recheck, sorted affected-offer locks, and active-job uniqueness. The retained SQL test is sequential. Gate report records session 74036 ending before 74041 began. | The required gate remains unproven for waits, rejection outcome, atomic closure, and deadlock behavior after the lock-protocol change. | Run one barrier-controlled two-session test through an explicitly authorized concurrency-capable path; retain timestamps, both outcomes, final invariant/event counts, and cleanup. Do not add infrastructure implicitly. | FIX NOW BEFORE USER APP | One targeted concurrent test/evidence record | Authorization for a concurrency-capable connection path | RESOLVED — genuine overlapping HTTPS race; one success, one `P0001`, one active assignment/event, zero residue |
| CR-009 | P2 | ROI-A | Customer eligibility; SECURITY RISK | [supabase/migrations/202608300008_gate_2_remediation.sql](C:/projects/biko/supabase/migrations/202608300008_gate_2_remediation.sql:201) — accept_offer customer checks | A suspended customer can still select a Driver | The caller is checked for auth.uid and order ownership, but only the Driver's profile status is validated. Hosted suspended-customer acceptance returned DRIVER_ASSIGNED. | Suspension blocks creation but not the later new-work assignment commitment. | Require ACTIVE CUSTOMER eligibility when accepting an offer. Preserve any deliberately allowed safe cancellation/history access separately. | FIX NOW BEFORE USER APP | Small RPC guard + negative test | None | RESOLVED — Wave A hosted targeted PASS (20260830133522) |
| CR-010 | P2 | ROI-A | Public Driver statistics; DATA-INTEGRITY RISK | [supabase/migrations/202608300008_gate_2_remediation.sql](C:/projects/biko/supabase/migrations/202608300008_gate_2_remediation.sql:404) — advance_order_state(COMPLETED); get_customer_order_offers | Completed-trip count never changes after completion | No implemented write updates drivers.completed_trip_count. Completion probe produced 1 completed order and stored_completed_trip_count=0; offer RPC returns that stored value (006:477). | The current privacy-safe read model publishes an inaccurate trust signal. | Choose one authority: update the count transactionally once per completion with a reconciliation/backfill rule, or derive the actual count in a measured set-based read. Do not invent ratings. | FIX NOW BEFORE USER APP | Small DB change + one completion/count regression | Preserve stale-transition/event guards | RESOLVED — Wave C hosted targeted PASS (`20260830143750`) |
| CR-011 | P2 | ROI-A | Driver waiting state; UX/STATE LOGIC ISSUE | [apps/driver_app/lib/features/driver/driver_screens.dart](C:/projects/biko/apps/driver_app/lib/features/driver/driver_screens.dart:608) — _OfferWaitingContentState.build / DriverService.loadOffer | Waiting details treat offer status as complete order truth | loadOffer reads raw latest offer status with no expiry/order state. ACTIVE renders waiting and withdrawal even when safe order lookup fails (error hidden at 677); SELECTED returns early before any order read. | The UI can instruct action on an elapsed/cancelled trip despite successful refresh. | Read one owned-offer summary with authoritative order status and expiry; derive waiting/selected/terminal display together. Keep terminal orders terminal and remove stale actions. | FIX NOW BEFORE USER APP | Small read-model/UI change + expiry/cancellation cases | No Realtime or scheduled expiry dependency | RESOLVED — Wave B hosted + Flutter targeted PASS |
| CR-012 | P2 | ROI-A | Driver read dependencies; ARCHITECTURE DEBT | [apps/driver_app/lib/features/driver/driver_service.dart](C:/projects/biko/apps/driver_app/lib/features/driver/driver_service.dart:17) — loadDashboard; DriverBootstrapScreen._restore; DriverProfileScreen | Active-trip restoration and Profile depend on the whole Dashboard | Future.wait combines requests, active order, and waiting offers; any failure rejects all data. Bootstrap awaits that aggregate (screens:1326), and Profile renders/signs out only in dashboard.data (1111–1193). | Optional feed failures disable unrelated core recovery and account controls. | Restore from the minimal account/active-order read; make optional feed failures local to their section. Profile/sign-out must not depend on available requests. Reuse existing services, without adding repository layers. | FIX NOW BEFORE USER APP | Small provider/service dependency split + partial-failure test | Coordinate with CR-017 | RESOLVED — Wave B targeted Flutter PASS |
| CR-013 | P2 | ROI-C | Read-error presentation; UX/STATE LOGIC ISSUE | [apps/driver_app/lib/features/driver/driver_ui.dart](C:/projects/biko/apps/driver_app/lib/features/driver/driver_ui.dart:196) — DriverErrorState | Known absence and session errors are rendered as no Internet | The required message parameter is discarded; build always returns RecoveryPhase.unavailable. loadOrder's explicit 'order no longer available' error and Auth failures take this same UI path. | Wrong recovery guidance hides terminal/permission/session outcomes. | Use a small typed read outcome for not-found/forbidden/auth/network; render the already-safe message and appropriate home/sign-in/retry action. Only catch authoritative absence as absence, not every DriverAppException. | FIX NOW BEFORE USER APP | Shared read-state mapping + Driver callers | Coordinate with CR-011 and CR-018 | RESOLVED — Wave B targeted Flutter PASS |
| CR-014 | P2 | ROI-A | Provider freshness; UX/STATE LOGIC ISSUE | [apps/driver_app/lib/features/driver/driver_providers.dart](C:/projects/biko/apps/driver_app/lib/features/driver/driver_providers.dart:28) — driverOrderProvider / driverOfferProvider; ActiveOrderScreen | Order-detail caches can survive indefinitely without a refresh path | Non-autoDispose families retain per-order results until explicit invalidation/sign-in/out. Active and Request detail data screens have no successful-state refresh action; no app-level resume invalidation exists. | Stale operational instructions and growing per-order caches in long sessions. | Add scoped re-entry/resume/manual refresh for the current operation, and dispose unused detail families or bound their lifetime. Show refresh/stale state without polling. | FIX BEFORE MAPS | Small provider/lifecycle/UI change | Keep session invalidation already present | RESOLVED — Milestone 8 targeted Flutter PASS |
| CR-015 | P2 | ROI-C | Waiting-offer pagination; LOGIC DEFECT | [supabase/migrations/202608300008_gate_2_remediation.sql](C:/projects/biko/supabase/migrations/202608300008_gate_2_remediation.sql:64) — get_driver_active_offers / WaitingOffersList._loadMore | Offset pagination skips live offers after earlier rows disappear | RPC uses LIMIT 50 OFFSET; client offset is currently rendered/deduplicated count (screens:1251). The filtered ACTIVE/window-valid set changes between pages. | A valid waiting offer is unreachable in that pagination pass. | Use the existing (created_at,id) order as a keyset cursor, or restart a clearly bounded snapshot when membership changes. No speculative index or paging framework. | FIX BEFORE REALTIME | Small RPC/caller cursor contract + page-boundary test | Coordinate with CR-016 | RESOLVED — Milestone 9 hosted 60-offer keyset boundary PASS (`20260830174205`) |
| CR-016 | P2 | ROI-C | Pagination race; UX/STATE LOGIC ISSUE | [apps/driver_app/lib/features/driver/driver_screens.dart](C:/projects/biko/apps/driver_app/lib/features/driver/driver_screens.dart:1234) — WaitingOffersList.didUpdateWidget / _loadMore | An old page response can repopulate a refreshed waiting list | didUpdateWidget clears _more but does not invalidate an in-flight request; _loadMore checks mounted only and appends against the new widget.offers. | Closed/obsolete offers reappear, and hasMore/error state can describe the previous dataset. | Capture a small generation value per request and discard results when first-page identity/session changes; reset paging errors/state with that generation. | FIX BEFORE REALTIME | Small widget-state guard + delayed-page test | None; compatible with CR-015 | RESOLVED — Milestone 9 delayed-page generation/loading seam PASS |
| CR-017 | P2 | ROI-A | Read amplification; PERFORMANCE/COST ISSUE | [apps/driver_app/lib/features/driver/driver_providers.dart](C:/projects/biko/apps/driver_app/lib/features/driver/driver_providers.dart:57) — runDriverMutation / DriverService.loadDashboard / loadOffer | Mutation reconciliation repeats account/order reads and unrelated tab invalidations | An online submit refresh does 4 Dashboard reads + 2 loadOffer reads + 1 order RPC, after the mutation: 8 HTTP requests total before SDK retries. History and Earnings separately repeat account + terminal-order reads. | CURRENT PROBLEM: unnecessary round trips, tail latency, and database/API usage; future Realtime invalidations would amplify this pattern. | Reuse the account/terminal data already loaded and reconcile only the affected order/offer plus availability/assignment summary. Derive Earnings from the same bounded terminal dataset. Consider one compact summary RPC only if it removes this measured fan-out. | FIX BEFORE REALTIME | Small-to-medium provider/read composition change | Coordinate with CR-012 | RESOLVED — Milestone 9 mutation-specific refresh; representative offer path 8 → 3 total requests; History/Earnings share one bounded dataset |
| CR-018 | P2 | ROI-A | Auth event handling; BUG | [packages/app_core/lib/app_core.dart](C:/projects/biko/packages/app_core/lib/app_core.dart:124) — AuthRouterRefresh; DriverApp.initState | Application Auth subscriptions do not handle stream errors | AuthRouterRefresh.listen and DriverApp's session listener have no onError. Installed GoTrue 2.27.2 emits addError for retryable token-refresh failures; its own SupabaseAuth listener explicitly handles that channel. | Unhandled zone errors occur outside the widget mutation catches; no intentional application recovery state is produced. | Handle the Auth error channel in each application subscription, retaining the session on retryable network failure and exposing safe recovery. Keep invalid-session sign-out distinct. | FIX NOW BEFORE USER APP | Small listener guards + stream-error test | Coordinate with CR-013 | RESOLVED — Wave B targeted Flutter PASS |
| CR-019 | P2 | ROI-C | Money presentation; BUG | [apps/driver_app/lib/features/driver/driver_screens.dart](C:/projects/biko/apps/driver_app/lib/features/driver/driver_screens.dart:510) — _CounterOfferSheetState.build / _ActiveOrderContentState.build | Two screens round real fractional fares to whole pounds | Lines 510 and 786 use toStringAsFixed(0), while backend prices have 2 decimal places and shared PriceDisplay/formatAmount preserves them. | Customers' proposals and completed agreed amounts are displayed inconsistently. | Use existing formatAmount/PriceDisplay at these two callers. Do not add another formatter. | FIX NOW BEFORE USER APP | Two call sites + one fractional-price assertion | None | RESOLVED — Wave C focused Flutter PASS |
| CR-020 | P2 | ROI-A | Assignment shape; DATA-INTEGRITY RISK | [supabase/migrations/202608300002_core_bidding_engine.sql](C:/projects/biko/supabase/migrations/202608300002_core_bidding_engine.sql:24) — orders / offers constraints; 008 composite FK | Relational constraints permit incomplete or contradictory assignment bundles | Active states do not require driver_id, selected_offer_id, agreed_price or assigned_at. The composite FK verifies same order only; it does not tie assigned driver/price to the selected offer. | Future trusted tooling can create operational states that the Flutter next-action mapping cannot safely interpret. | Add only state-shape CHECKs/FKs that encode current invariants, plus an explicit trusted assignment consistency check if needed. Preflight existing rows. Do not build a generic transition engine. | FIX BEFORE PUBLIC LAUNCH | Focused constraints/trusted-write checks + negative tests | Inventory legacy rows and define legitimate recovery exceptions | PARTIALLY RESOLVED — Wave A assignment bundle/FK invariants PASS; trusted recovery/legacy exception review remains before Public Launch |
| CR-021 | P2 | ROI-A | Backend regression assets; TESTABILITY GAP | [supabase/tests/gate_2_remediation.sql](C:/projects/biko/supabase/tests/gate_2_remediation.sql:1) — Retained SQL test coverage | Most completed backend contracts have evidence but no retained runnable regression | The only SQL test file covers A/B/D/E/L remediation. The 6.1/7A delivery bounds, cancellation matrix, withdrawal ownership, customer offer privacy, and deadline tests exist as narrative evidence, not retained test code. | Expensive rediscovery and false confidence when effective functions are replaced across milestones. | Retain a compact rollback-based contract test per changed trust boundary, including mixed-role and deadline negatives. Run affected cases, not a full gate every time. | FIX NOW BEFORE USER APP | Small focused SQL regression assets | Use known hosted contract; no new test infrastructure | PARTIAL — Wave A/B/C retained focused SQL assets improved coverage; broader 6.1/7A regression asset scope remains |
| CR-022 | P2 | ROI-A | Flutter contract tests; TESTABILITY GAP | [apps/driver_app/test/gate_2_remediation_test.dart](C:/projects/biko/apps/driver_app/test/gate_2_remediation_test.dart:27) — FakeDriverService / router / mount | Fakes bypass the real service, router, and failure composition | Fake loadOrder returns currentOrder regardless of ID; fake mutations directly change memory before throwing; the custom router bypasses createAuthRouter and DriverApp's Auth listeners. No HTTP projection/grant/error mapping is exercised. | Recovery/navigation behavior has useful unit evidence but lacks integration at the exact seams most likely to regress. | Keep the good tests; add a small mocked-HTTP service contract test and one configured root-router Auth/session test. Include commit-later and never-settling reads, not only commit-before-timeout. | FIX NOW BEFORE USER APP | Several focused existing-framework tests | CR-004/005/018 contracts | PARTIAL — Wave B added SDK/router/timeout seam checks; broader configured integration coverage remains |
| CR-023 | P2 | ROI-C | Diagnostics; MAINTAINABILITY PROBLEM | [packages/app_core/lib/operation_recovery.dart](C:/projects/biko/packages/app_core/lib/operation_recovery.dart:132) — MutationReconciler.retryRead; DriverService._friendly | Recovery loses the failing read context and operation correlation | The read catch changes phase but never retains its failure/stack; error remains the earlier mutation error or null. The controller receives anonymous callbacks, and _friendly replaces backend errors with text-only BusinessFailure. | Hard-to-reproduce recovery incidents require guesswork; business-message wording is also being used as the client's contract. | Retain a sanitized last-read error/code and operation name/order ID; add a lightweight diagnostic hook using existing logging. Preserve technical codes separately from safe Arabic messages. Do not log tokens, passwords, phone, or payloads. | FIX BEFORE PUBLIC LAUNCH | Small structured error/context fields | Coordinate with CR-013/018 | OPEN — REVIEW ONLY |
| CR-024 | P2 | ROI-A | Authoritative documentation; MAINTAINABILITY PROBLEM | [docs/BUSINESS_RULES_FREEZE_V1.md](C:/projects/biko/docs/BUSINESS_RULES_FREEZE_V1.md:161) — Implementation snapshot; Gate 2 report and issue register | Current-state claims remain mixed with obsolete milestone snapshots | Freeze section 16 and issue-register Planned Gaps still call 90 seconds, parcel fields and other completed deltas pending. Older issue rows state removed implementation defects in present tense; appended remediation sections correct them. | Wrong entry-gate decisions, duplicate migrations/work, and avoidable implementation drift. | After the authorized gate decision, perform the already-planned focused implementation-status sync. Label historical evidence clearly and keep one current status per issue without changing frozen business rules. | FIX NOW BEFORE USER APP | Documentation-only sync selected by PM | Existing SVG2-010 / final Gate 2 decision | RESOLVED — Final Evidence Closure current-status reconciliation complete |
| CR-025 | P2 | ROI-D | Customer offer list; PERFORMANCE/COST ISSUE | [supabase/migrations/202608300006_user_flow_backend_delta.sql](C:/projects/biko/supabase/migrations/202608300006_user_flow_backend_delta.sql:445) — get_customer_order_offers | Customer offer RPC has no explicit bounded paging contract | The joined list ends with ORDER BY only, without limit/cursor. An infrastructure row cap, if reached, has no continuation contract in the RPC. | FUTURE SCALE RISK: payload and sort work grow with offers; no present-volume performance failure is claimed. | Define a modest explicit page limit and stable cursor when candidate volume warrants it; expose enough continuation information. Do not add an index without a measured plan. | SAFE TO DEFER | Small read-contract change when volume requires | Measured candidate/offer volume | OPEN — REVIEW ONLY |
| CR-026 | P2 | ROI-C | Offer eligibility reads; LOGIC DEFECT | [supabase/migrations/202608300006_user_flow_backend_delta.sql](C:/projects/biko/supabase/migrations/202608300006_user_flow_backend_delta.sql:483) — get_customer_order_offers vs accept_offer | Customer offer list includes an offline Driver that selection rejects | Listing checks ACTIVE Driver/profile/office but not is_online or candidate membership. Hosted probe returned one offer after its Driver successfully went offline; accept_offer requires is_online and current candidacy. | Avoidable deterministic selection failures even without a timing race. | Align the preselection filter with current selection eligibility or explicitly expose a nonselectable availability state. Do not silently alter the approved multiple-offer model. | FIX NOW BEFORE USER APP | Small read predicate + offline-offer test | Keep Driver availability semantics distinct from active-trip permission | RESOLVED — Wave C hosted targeted PASS (`20260830143750`) |
| CR-027 | P2 | ROI-A | Release signing; SECURITY RISK | [apps/driver_app/android/app/build.gradle.kts](C:/projects/biko/apps/driver_app/android/app/build.gradle.kts:33) — buildTypes.release in both apps | Android release builds explicitly use debug signing | Both release blocks set signingConfig = signingConfigs.getByName('debug'). | It is not a production signing identity/update chain; publishing readiness can be mistaken for a successful release build. | Configure protected release signing in the release workflow and fail release publishing if it is missing. Keep debug signing for debug development. | FIX BEFORE PUBLIC LAUNCH | Small configuration + secret-management setup | Owner-controlled signing credentials and distribution decision | OPEN — REVIEW ONLY |
| CR-028 | P3 | ROI-D | Shared presentation; CODE QUALITY IMPROVEMENT | [packages/app_core/lib/order_presentation.dart](C:/projects/biko/packages/app_core/lib/order_presentation.dart:32) — PriceDisplay / RouteSummary / status colors | Extracted components still duplicate Driver-specific palette values | Shared widgets hardcode the same blue/slate/semantic values as DriverColors, while User App seeds teal. Changing the app theme cannot change several shared presentation colors. | MEDIUM-VALUE duplication: visible theme drift and multiple edit sites, not a current security or flow failure. | Use Theme/ColorScheme for app accents and text; centralize only truly shared semantic colors when these components are next touched. | SAFE TO DEFER | Small shared-widget cleanup | User/Driver accent decision | OPEN — REVIEW ONLY |
| CR-029 | P3 | ROI-C | RPC/model contract; CODE QUALITY IMPROVEMENT | [apps/driver_app/lib/features/driver/driver_service.dart](C:/projects/biko/apps/driver_app/lib/features/driver/driver_service.dart:175) — advanceOrder / DriverOrder.fromJson | advanceOrder returns a partially populated DriverOrder that callers discard | Lifecycle RPC returns a raw orders composite without service_types/service_code/name. fromJson silently defaults serviceCode='' and serviceName='خدمة'. Current caller at screens:754–756 discards the returned object. | A misleading extension point hides a projection mismatch and performs unnecessary parsing now. | Return Future<void> for the currently discarded mutation result, or explicitly name a minimal mutation result when a real caller needs it. Avoid inventing duplicate domain models. | SAFE TO DEFER | Small signature/caller cleanup | None | OPEN — REVIEW ONLY |
| CR-030 | P3 | ROI-C | Foundation script; TESTABILITY GAP | [scripts/check-foundation.ps1](C:/projects/biko/scripts/check-foundation.ps1:37) — focused forbidden Auth/SMS OTP patterns | The OTP text ban could reject approved operational wording | The structural gate now scans only executable implementation/migration text for forbidden login/SMS/Auth OTP APIs and configuration, while the product uses no Ride or Pickup OTP and one final Delivery Confirmation Code. | BRITTLE TEST: keep this lightweight check focused on authentication policy; operational code validation belongs to the Milestone 10 hosted contract. | Scope the check to forbidden login/SMS APIs/configuration or replace it with a focused Auth contract assertion. | RESOLVED — Milestone 10 targeted gate passed |
| CR-031 | P3 | ROI-D | Index maintenance; PERFORMANCE/COST ISSUE | [supabase/migrations/202608290001_foundation.sql](C:/projects/biko/supabase/migrations/202608290001_foundation.sql:139) — office_members_office_user_idx | One office-members index exactly duplicates a unique index | office_members UNIQUE(office_id,user_id) at line 63 already creates the same ordered B-tree key as the explicit index at line 139. | CURRENT but small storage/write overhead; not a material hot-path incident at current volume. | If doing an index-maintenance wave, verify catalog equivalence and remove only the duplicate explicit index in a new migration. Do not edit foundation history. | SAFE TO DEFER | One catalog check + focused index migration | None; no new index recommended | OPEN — REVIEW ONLY |
| CR-032 | P3 | ROI-C | Async UI boundaries; CODE QUALITY IMPROVEMENT | [apps/driver_app/lib/features/driver/driver_screens.dart](C:/projects/biko/apps/driver_app/lib/features/driver/driver_screens.dart:43) — Home/Waiting/History refresh; Profile sign-out; shared Home sign-out | Manual refresh and sign-out callbacks allow Future errors to escape | Refresh callbacks await provider futures without catch (43, 536, 952); sign-out callbacks return an unhandled Future (1180, app_core:407). Installed GoTrue removes the local session first, then remote revocation may throw. | Uncaught async diagnostics and no explicit completion/error handling. This is not a claim that failed remote revocation keeps the local user signed in. | Consume these UI Future failures with safe state/diagnostic handling; retain existing provider error rendering and local logout behavior. Add busy guards only where repeated actions cause duplicate reads. | SAFE TO DEFER | Small callback-boundary cleanup | Reuse CR-013/023 behavior | OPEN — REVIEW ONLY |

## Quick Wins

These are small, high-value candidates—not changes made during review.

1. **CR-007:** explicit INTERNET permission in both main manifests; inspect merged release output once.
2. **CR-019:** replace two whole-pound format calls with existing formatAmount.
3. **CR-013:** stop discarding safe read-error messages; distinguish no-longer-available from no Internet.
4. **CR-016 (resolved in Milestone 9):** reject old page results with a generation check after first-page refresh.
5. **CR-018:** handle Auth-stream errors in the two application listeners.
6. **CR-026:** align customer offer eligibility with selection's online/candidate requirements.
7. **CR-032:** consume asynchronous refresh/logout errors without claiming a failed remote logout retains the local session.

Security helper changes CR-001/002 are compact but require deliberate authorization tests; do not treat them as cosmetic quick wins. CR-003 must check time **after** lock waits, not just rename now() at the old early check.

## Before User App

Select a focused boundary-remediation wave before expanding User Core:

- Authorization: **CR-001, CR-002, CR-009**.
- Bidding deadline and outstanding gate evidence: **CR-003, CR-008**.
- Stable create-intent and bounded recovery: **CR-004, CR-005**.
- Shared Auth reset/error channel and read outcome semantics: **CR-006, CR-013, CR-018**.
- Correct offer waiting/list trust data: **CR-010, CR-011, CR-026**.
- Make restoration independent of optional feeds: **CR-012**.
- Reuse accurate price formatting: **CR-019**.
- Retain/extend only the affected high-value tests: **CR-021, CR-022**.
- Complete the already-planned current-status documentation sync at the gate decision: **CR-024 / SVG2-010**.

Milestone 7A is already implemented. Do not rebuild it from stale documentation. Existing Gate 2 remains unclosed until the authorized race evidence and status-sync requirements are handled. Completion of this review does not authorize User App implementation.

## Before Maps

- **CR-014:** current operation refresh on entry/resume/manual request; bounded detail-provider lifetime.
- Carry forward the preceding authorization/recovery fixes. Coordinates exist in PostgreSQL; add the actual operational projection/model when Maps consumes it.
- Keep PostGIS candidate discovery, selected-route API work and location-state ownership separate. No Maps feature or speculative index is requested by this review.

## Before Realtime — Resolved in Milestone 9

- **CR-015:** stable waiting-offer keyset cursor — resolved.
- **CR-016:** generation protection against stale page responses — resolved.
- **CR-017:** narrow invalidation and shared bounded terminal reads — resolved.
- Realtime notifications now trigger scoped authoritative refresh only; they never authorize client-owned mutations.
- History, Profile, Earnings, and Driver location writes have no Realtime subscriptions.

## Before Production

Items explicitly timed FIX BEFORE PUBLIC LAUNCH:

- **CR-007:** main Android INTERNET permission and release-network acceptance.
- **CR-020:** minimal trusted assignment-shape integrity constraints/checks.
- **CR-023:** redacted operation/read-failure diagnostics using existing facilities.
- **CR-027:** protected release signing instead of debug signing.
- **CR-030:** replace the blanket OTP text ban before approved safety work.

Also close applicable earlier blockers and the existing SVG2-013 Auth/Android runtime evidence limitation. Run a focused release-device journey, not another blanket gate merely for freshness. Production Auth/SMTP/redirect/MFA and launch safety/legal decisions are separate scheduled gates, not verified here.

## Safe To Defer

- **CR-025:** explicitly paged customer offers when candidate volume warrants it; do not invent a high-volume incident now.
- **CR-028:** theme-token cleanup when shared components are next changed.
- **CR-029:** misleading unused lifecycle return cleanup at the next service-contract touch.
- **CR-031:** exact duplicate index removal in an authorized maintenance migration.
- **CR-032:** small refresh/logout callback hygiene after higher-impact recovery fixes.
- Unreferenced Dashboard SVGs, empty iOS scaffold tests, scaffold wording, and a leaf enums file can wait. No issue count was inflated for each unused asset or long file.
- Ratings, remaining Maps/Places/ETA and Realtime/FCM external configuration, progressive dispatch, commissions/ledger, cooldown, zones, and advanced Dashboard operations remain **FUTURE FEATURE — NOT A DEFECT**. Milestone 10 document/helmet/geofence/final-Delivery-code behavior is complete.

## Do NOT Refactor Yet

- Do not rewrite UI → Riverpod → Service → Supabase into repositories/use cases/interfaces with one implementation.
- Do not replace PostgreSQL RPCs with Edge Functions or direct client table writes.
- Do not alter the current explicit state machine into a configurable generic workflow engine.
- Do not remove locking, constraints, ownership checks, or uncertain-outcome blocking to shorten code.
- Do not edit, squash or rename already-applied migrations. Plan any future baseline/history reconciliation separately.
- Do not build a universal User/Driver shell or giant app_core model. Share only actual common domain/presentation/recovery behavior.
- Do not split driver_screens.dart solely because of line count; separate an existing screen when a concrete change warrants it.
- Do not introduce Redis, paid observability, event buses, polling, new routing providers, or speculative indexes.
- Do not turn current gross display doubles into a ledger or implement commission percentages early.
- Do not rewrite good targeted tests; add missing boundary cases and preserve their honest scope.
- Do not repeat the same serialized Supabase-tool race and call it concurrency. CR-008 requires a separately authorized capability.

## Suggested Selection Boundaries

These are PM decision aids, not new tasks or authorization:

1. **Trust and bidding:** CR-001/002/003/009 plus their exact negative tests.
2. **Recovery contract before User expansion:** CR-004/005/006/011/012/013/018 with focused service/router tests.
3. **Read correctness and small UI wins:** CR-010/019/026; retain missing boundary regressions.
4. **Evidence closure:** CR-008 and then the authorized CR-024 status sync; do not claim Gate 2 PASS before overlap is proven.
5. **Maps/Realtime preparation:** CR-014/015/016/017 when those boundaries approach.
6. **Release hardening:** CR-007/020/023/027/030 and focused device/configuration evidence.

Production code changed by this review: **NO**.

Next: **WAIT FOR PROJECT MANAGER TO SELECT REMEDIATION WAVE.**
