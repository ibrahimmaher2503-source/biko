# Current Issues Register

System Validation Gate 2 current result: **PASS WITH ISSUES**. CR-008/SVG2-001 now has genuine overlapping application-path concurrency evidence, CR-024/SVG2-010 is reconciled, Milestone 7A remains complete, and User App Core is unblocked. The problem/evidence columns below preserve historical findings; the Status column and final closure section are authoritative for current state.

| ID | Severity | Area | Problem | Evidence | Impact | Fix Timing | Recommended Fix | Status |
|---|---|---|---|---|---|---|---|---|
| SVG2-001 | P1 | Backend / assignment integrity | One driver can hold two active jobs. `accept_offer` does not reject an existing active assignment and no active-driver partial uniqueness constraint exists. | Hosted rollback: the same Driver A was selected by Customer A and Customer B; both calls succeeded and active-order count was 2. | Double booking, conflicting Driver UI/state, and operational/data corruption risk. | FIX BEFORE USER APP | In a new migration, keep the driver-row lock, check active orders after acquiring it, add a partial unique invariant for active `orders.driver_id`, return a clean conflict, exclude active drivers from candidate/request/offer paths, and close or invalidate their other ACTIVE offers on assignment. | RESOLVED — overlapping HTTPS race PASS; one winner, one deterministic conflict, one active job/event |
| SVG2-002 | P1 | Driver App / bidding contract | Backend permits one ACTIVE offer per Driver/Order, therefore multiple cross-order offers; Driver App stores and loads only one latest `waitingOffer`. | `DriverService._loadWaitingOffer()` orders then `limit(1)`; `DriverDashboardData.waitingOffer` is scalar; hosted Driver A created two simultaneous cross-order offers. | Older offers are hidden and cannot be monitored/withdrawn; a later selection can surprise the driver and amplifies double assignment. | FIX BEFORE USER APP | Decide the frozen cross-order contract. Prefer modeling a bounded list of active offers and closing all other offers on assignment; if product instead wants one global offer, enforce that in PostgreSQL/RPC and UI. | RESOLVED — see remediation evidence below |
| SVG2-003 | P1 | Driver App / network reliability | Mutation failures do not reconcile authoritative state when the backend may have committed before the client received the response. | Submit, withdraw, availability, lifecycle, and cancel catch paths show a snackbar only; provider invalidation/refresh happens only on success. | Stale CTAs/screens and unsafe user retries after timeouts or connection loss; serious pattern would be copied into User App. | FIX BEFORE USER APP | Add one minimal shared uncertain-outcome pattern: classify transport/session failures, disable blind automatic retry, invalidate and await the relevant order/offer/dashboard state, then route/render from the refreshed server result. Add one targeted service/widget check. | RESOLVED — see remediation evidence below |
| SVG2-004 | P2 | Database integrity | `orders.selected_offer_id` only references `offers.id`; it does not ensure the selected offer belongs to the same order. | A trusted rollback update attached the normal order's selected offer to a different cancelled order and succeeded. Direct clients cannot perform this update, but the relational invariant is absent. | Future trusted jobs/imports/admin recovery can create split relational state. | FIX BEFORE USER APP | Add a same-order composite FK/constraint design in a new migration and preserve the RPC's atomic update order. | RESOLVED — see remediation evidence below |
| SVG2-005 | P2 | Shared Auth UX | Shared email/password UI displays raw `AuthException.message`. | `packages/app_core/lib/app_core.dart` assigns `error.message` directly, contrary to the Arabic error-state rule. | English/backend details and inconsistent messages will be duplicated into User App. | FIX BEFORE USER APP | Add a small shared Auth error mapper for wrong password, invalid email, rate limit, network, and generic failure; keep technical details out of UI. | RESOLVED — see remediation evidence below |
| SVG2-006 | P2 | Flutter architecture | Reusable order status labels/colors, price formatting, route/status presentation primitives, and date formatting live in Driver-only files. | `driver_ui.dart` owns `StatusBadge`, `PriceDisplay`, `RouteSummary`, `orderStatusLabel`, colors, and formatting while User App will need overlapping semantics. | User App will duplicate domain presentation and drift unless the minimum shared layer is established first. | FIX BEFORE USER APP | Move only cross-app status/price/route primitives and pure formatters to `app_core`; keep Driver-specific screens and actions local. | RESOLVED — see remediation evidence below |
| SVG2-007 | P2 | Driver UI states | Dedicated No Internet, Retrying/Reconnecting, and loading-skeleton states required by the UI specifications are absent; generic spinner/error is used. | UI specifications enumerate these states/components; implementation has `CircularProgressIndicator`, generic `DriverErrorState`, and a generic connectivity message only. | Poor recovery clarity during mobile network changes and likely duplicated ad-hoc handling in User App. | FIX BEFORE USER APP | Create the smallest shared connectivity/retry presentation contract and reuse it in both apps; retain manual retry and no automatic sensitive mutation retry. | RESOLVED — see remediation evidence below |
| SVG2-008 | P2 | Driver active-trip recovery | Reopen restores the active order as a Home card but does not restore/navigate to the correct Active Trip screen. | Signed-in router always starts `/home`; dashboard fetches active order and shows a card. Requirements say reconnect/reopen restores the correct screen/state. | Extra step during an active operation and weaker recovery after process death. | FIX BEFORE PUBLIC LAUNCH | After authoritative bootstrap, redirect to `/active-order/:id` when an active assignment exists; preserve Home fallback and avoid local-only assumptions. | RESOLVED — see remediation evidence below |
| SVG2-009 | P2 | Privacy / data minimization | Candidate RLS grants SELECT on the full allowed order row, so a custom authenticated client can request `customer_id` even though Driver UI does not need it. | DriverService projection is safe and omits the field; hosted RLS still makes the row, including the customer UUID, selectable to a candidate. Customer profile/phone remained hidden. | Avoidable internal identifier exposure and cross-surface coupling. | FIX BEFORE PUBLIC LAUNCH | Expose candidate requests through a safe view/RPC with explicit columns and revoke direct candidate order-row access where feasible. | RESOLVED — see remediation evidence below |
| SVG2-010 | P2 | Documentation drift | Current documentation contradicts hosted 6.1 and the gated milestone sequence. | Freeze/progress pending snapshots still list offer withdrawal and required events; older specs retain pending labels; progress names generic Milestone 7; UI Auth states mention login OTP despite DEC-001. | Future implementation may duplicate work, regress decisions, or start the wrong slice. | FIX BEFORE USER APP | Run one focused documentation sync after blocker decisions: update implementation snapshots/status labels and next-milestone wording without changing frozen product behavior. | RESOLVED — CR-024 focused current-status reconciliation complete |
| SVG2-011 | P3 | Performance / API chatter | History and Earnings repeat account + terminal-order reads; offers/history plans use bounded sorts. | Online Home is 4 calls; History and Earnings are 2 each. All hosted plans used indexes and no unbounded sequential scan; lists are limited to 20/50. | Minor avoidable calls/sorts at future scale; no present load evidence. | SAFE TO DEFER | Reuse a cached account/terminal-order provider and add a partial/order index only after production-like row counts or query metrics show need. | OPEN |
| SVG2-012 | P3 | Maintainability | Driver screens are concentrated in one 1,272-line file. | Static file-size inspection; service/models/providers remain separate and call flow is still clear. | Slower navigation/review as more Driver features arrive. | SAFE TO DEFER | Split by existing screen boundaries when the file next receives material feature work; do not introduce new abstractions. | OPEN |
| SVG2-013 | P3 | Runtime evidence | Gate environment had no Android device/emulator or `adb`, and the connected Supabase capability exposes database operations but not an Auth HTTP/key path. Operational screens used hosted RPC + widget evidence; prior Auth smoke covered valid signup/login/refresh/sign-out but not an explicitly recorded wrong-password attempt. | `flutter devices` exposed Windows/Chrome/Edge; Driver App has Android/iOS targets; targeted analyze and 7 tests passed; the no-key Auth probe returned 401 without creating a user. | Current-gate wrong-password/session-loss HTTP behavior and Android OS lifecycle, process restore, keyboard, and touch behavior remain unproven. | FIX BEFORE PUBLIC LAUNCH | After P1 fixes, run one focused Android hosted journey with the approved client configuration: wrong password, sign in/restore, active-trip restore, lifecycle, timeout/reconnect, cancellation, and sign out/session-loss redirect. | OPEN — VALIDATION LIMITATION |

## Planned Gaps — Not Current Defects

- Suggested Price and 70% proposal floor.
- Progressive 2/4/6/8 km PostGIS dispatch; the server-owned 90-second bidding window is implemented.
- Milestone 10 is implemented: Driver/motorcycle documents, expiry, age,
  helmet eligibility, trusted geofences, and one final Delivery Confirmation
  Code. Ride and Delivery Pickup use no OTP.
- Cancellation reason taxonomy, rolling cooldown/flags, and Book Again.
- Independent/Office commissions, promotion, ledger, settlement, and balances.
- Operating zones and external Realtime/push configuration; operations/Admin
  review UI and trusted daily document-expiry invocation.

These remain scheduled future behavior unless their milestone owner explicitly changes the plan.


## Remediation Evidence (2026-08-30)

Historical problem descriptions above are retained. Current implementation:

- Hosted migration: `20260830021017_gate_2_remediation`, local `202608300008_gate_2_remediation.sql`.
- SVG2-001: partial unique active-job index and Driver-lock/recheck acceptance implemented. A/B/D hosted checks passed, including trusted/manual duplicate-assignment rejection, atomic cross-order offer closure, unavailable-after-assignment, candidate exclusion, submit rejection, and one representative lifecycle action while unavailable. The later Final Evidence Closure below supersedes the earlier serialized attempt and resolves the issue with genuine overlap.
- SVG2-002: multiple cross-order ACTIVE offers are the approved contract; one per Driver/Order. Driver Home renders a paged 50-offer collection with route summaries and individual withdrawal. Hosted A/B and targeted Driver F checks passed.
- SVG2-003: shared `MutationReconciler` classifies success/business/uncertain/session outcomes; all eight Driver mutation actions use one authoritative refresh path. No mutation is replayed. Lost-submit and lost-lifecycle checks passed, including unavailable -> manual read retry -> recovered state.
- SVG2-004: composite selected-offer/order FK rejects a trusted cross-order reference; normal acceptance passed.
- SVG2-005: shared safe Arabic Auth mapper covers credentials, email, existing account, confirmation, rate limit, transport, session, and generic errors; shared Auth UI uses it.
- SVG2-006: shared status labels/colors, amount/date formatting, RouteSummary, PriceDisplay, StatusBadge, and ServiceTypeBadge are consumed by Driver UI. Shared presentation widget check passed.
- SVG2-007: shared checking/unavailable/read-retry/recovered states and loading skeleton exist; Driver sensitive actions remain blocked until authoritative reads succeed. Riverpod automatic read retries are disabled for these providers.
- SVG2-008: authenticated startup uses an authoritative bootstrap route. Active assignment -> Active Trip; absent/terminal assignment -> Home, without redirect loops. All three targeted cases passed.
- SVG2-009: explicit-column `get_driver_requests` replaces candidate raw-order SELECT access; customer UUID/private identity are absent, non-candidate access denied, assigned Driver reads preserved. Hosted L/security-delta checks passed.
- SVG2-010: resolved by the focused CR-024 current-status reconciliation recorded below.
- SVG2-011 through SVG2-013 remain OPEN at their existing timing; they were not implemented or revalidated.

Verification totals: 5 shared-core checks and 7 Driver checks passed. Full `flutter analyze --no-pub` ran once per affected scope; its five lint findings were corrected, then only the flagged files/providers were checked with `dart analyze` (no issues). Driver failures were corrected and only the failed scope/case was rerun. No dependencies changed and no `flutter pub get` ran.

Concurrency evidence limitation: session 74036 ran 02:14:56.120671–02:14:59.130297 UTC and succeeded; session 74041 began 02:15:01.173019 UTC and rejected the now-closed offer. These timestamps prove sequential execution, **not a concurrency PASS**. No async-session database extension is installed. Additional test capability requires approval before completing Test C.

Cleanup: the main hosted test used ROLLBACK; the short-lived race fixture was explicitly deleted. Audit returned zero test Auth users, profiles, Drivers, orders, offers, and events. Runnable focused checks remain in `supabase/tests/gate_2_remediation.sql` and the two `gate_2_remediation_test.dart` files.

## Final Evidence Closure

- CR-008/SVG2-001: two independent authenticated HTTPS clients used the hosted publishable application path and released `accept_offer` through one barrier. B ran `14:46:29.735678–14:46:30.817925 UTC`; A ran `14:46:29.736612–14:46:30.818234 UTC`. The intervals genuinely overlap.
- Result: A returned HTTP 200. B returned HTTP 400 / `P0001`, `Driver already has an active assignment`. Final truth was one active job, one assigned order, one complete assignment bundle, one SELECTED offer, one CLOSED competing offer, one untouched BIDDING losing order, and one `DRIVER_ASSIGNED` event. No deadlock occurred.
- Cleanup: reserved CR-008 Auth users, profiles, Driver, orders, offers, candidates, Delivery details, and order events all returned zero.
- CR-006 remains implemented with **manual hosted redirect allowlist confirmation required** for `biko-driver-dev://auth-callback/` and `biko-user-dev://auth-callback/`; this does not block User App Core under the approved closure rule.
- CR-024/SVG2-010: resolved. Current status is synchronized without deleting original Gate evidence or changing frozen product behavior.
- Current decision: **System Validation Gate 2 = PASS WITH ISSUES; User App Core = UNBLOCKED**.
